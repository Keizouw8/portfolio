<style>
    :root{
        --white: #E3D8BB;
        --black: #231C11;
        --yellow: #E1B44B;
        --red: #C04D3F;
        --blue: #315F82;
        --w: 0;
        --h: 0;
    }

    body{
        -ms-overflow-style: none;
        scrollbar-width: none;
        background: var(--white);
        color: var(--black);
        overflow-x: hidden;
    }

    ::-webkit-scrollbar {
       display: none;
    }

    h1{
        color: var(--black);
        font-family: pixel;
        font-size: 10vh;
    }
    
    h2{
        color: var(--black);
        font-family: pixel;
        font-size: 3.5vh;
    }

    poster{
        position: absolute;
        background: var(--blue);
        height: 68vh;
        width: calc(100vw - 12vh);
        left: 6vh;
        top: 6vh;
    }

    #eye{
        position: relative;
        max-width: calc(100vw - 18vh);
        max-height: 62vh;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
    }

    #iris, #pupil{
        transition: 0.1s;
    }

    poster #school{
        color: var(--white);
        position: absolute;
        transform: rotate(-90deg) translate(-46%, -400%);
        transform-origin: 50% 50%;
        top: 0;
        left: 0;
    }

    poster #work{
        color: var(--white);
        position: absolute;
        transform: rotate(-90deg) translate(38%, 40%);
        transform-origin: 50% 50%;
        bottom: 0;
        right: 0;
    }

    #landing #name{
        color: var(--black);
        font-family: pixel;
        font-size: 10vh;
        margin: 0;
        padding: 0;
        position: absolute;
        left: 6vh;
        top: 79vh;
        line-height: 0.7;
    }

    #landing #name #last{
        font-size: 8vh;
    }

    #landing #occupations{
        margin: 0;
        padding: 0;
        position: absolute;
        right: 6vh;
        top: 79vh;
        line-height: 1;
        text-align: right;
    }

    @media screen and (max-aspect-ratio: 68/100) {
        poster #work, poster #school, #landing #occupations {
            display: none;
        }
    }
</style>